import { LinkIsActiveLinkPipe } from './link-is-active-link.pipe';

describe('LinkIsActiveLinkPipe', () => {
  it('create an instance', () => {
    const pipe = new LinkIsActiveLinkPipe();
    expect(pipe).toBeTruthy();
  });
});
