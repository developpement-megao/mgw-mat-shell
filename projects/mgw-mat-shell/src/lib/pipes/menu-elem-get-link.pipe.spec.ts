import { MenuElemGetLinkPipe } from './menu-elem-get-link.pipe';

describe('MenuElemGetLinkPipe', () => {
  it('create an instance', () => {
    const pipe = new MenuElemGetLinkPipe();
    expect(pipe).toBeTruthy();
  });
});
