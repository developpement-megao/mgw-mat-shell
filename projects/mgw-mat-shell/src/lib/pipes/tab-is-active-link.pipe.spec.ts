import { TabIsActiveLinkPipe } from './tab-is-active-link.pipe';

describe('TabIsActiveLinkPipe', () => {
  it('create an instance', () => {
    const pipe = new TabIsActiveLinkPipe();
    expect(pipe).toBeTruthy();
  });
});
