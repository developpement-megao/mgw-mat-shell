import { MenuElemGetGroupPipe } from './menu-elem-get-group.pipe';

describe('MenuElemGetGroupPipe', () => {
  it('create an instance', () => {
    const pipe = new MenuElemGetGroupPipe();
    expect(pipe).toBeTruthy();
  });
});
